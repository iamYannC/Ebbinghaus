# Ebbinghaus Benchmark — Agent Instructions

## Rules

1. **Human-review stamp is human-only.**
   The line `# This file was generated by AI and was reviewed by a human. yannco5@gmail.com or https://iamyannc.github.il/Yann-dev` is a stamp that only the human adds after reviewing a file. Agents must never add this stamp themselves. If an agent modifies a file that contains this stamp, the agent must remove it — the file is no longer human-reviewed.

## Project overview

An R-based benchmark for testing how AI vision-language models perceive the Ebbinghaus illusion. Three-phase pipeline: stimulus creation, model evaluation, analysis.

## Working directory

All `source()` calls and file paths are relative to the project root (`Ebbinghaus/`). Set your working directory there before running anything.

## Key files

| Path | Purpose |
|------|---------|
| `config/defaults.R` | All configurable parameters (pools, ranges, constants) |
| `R/generate_design.R` | Entry point for Phase 1 — sources all dependencies |
| `R/generate_trial.R` | Single trial generator (sources verify_trial, classify_tier, defaults) |
| `R/render_stimuli.R` | Batch renderer (sources draw_trial → draw_shape) |
| `R/evaluate.R` | Phase 2 — vitals-based evaluation (custom solver/scorer/orchestrator) |
| `R/analyze.R` | Phase 3 — joins data, computes 14 metrics (accuracy, d-prime, confusion, etc.), generates 12+ plots |
| `R/strip_answer.R` | Strips ground truth from image filenames for evaluation |
| `README.md` | Project overview, tier definitions, usage examples |
| `VARIABLE_REGISTRY.md` | All variables, schemas, phase I/O, combinatorics |
| `docs/reference_manual.pdf` | Roxygen-generated PDF reference for all functions |

## Phase I/O

| Phase | Input | Output |
|-------|-------|--------|
| 1. Stimulus Creation | `source("R/generate_design.R")` | `trials` data frame + images in `images/` |
| 2. Evaluation | `trials` (or `data/trials.csv`), `data/prompts.csv`, API keys | vitals JSON logs (viewable via `vitals_view()`) |
| 3. Analysis | `data/trials.csv`, vitals Task objects (or legacy `data/evals.csv`), `data/prompts.csv` | Summary tables + plots in `output/` |

## Data schemas

### trials.csv (32 columns)
- **ID:** `trial_id`, `n_comparisons` (fixed at 2)
- **Layout:** `orientation`, `canvas_width`, `canvas_height`, `background_color`
- **Test stimuli (A and B):** `test_{a,b}_{shape,size,color,fill}`
- **Surround stimuli (A and B):** `surround_{a,b}_{shape,size,n,color,fill,distance}`
- **Ground truth:** `true_larger`, `true_diff_ratio` (decimal, not pct), `tier` (0/1/2/3/NA)
- **Reproducibility:** `seed`
- **Rendering:** `file_format`, `file_path`

### Key conventions
- `true_diff_ratio` is a decimal proportion `[0, 1)`, not a percentage.
- `tier` parameter in `generate_trial()`: `NULL` = unconstrained, `0`/`1`/`2`/`3` = targeted. `NA` is only an output of `classify_tier()`, never a valid input.
- `generate_design()` is a pure function — returns the data frame without writing to disk.
- Seed auto-generation uses `round(runif(1, -1e9, 1e9))`, not `sample(-1e9:1e9, 1)` (the latter materializes a 2B-element vector).

## Dependencies

Phase 2 requires the `vitals` package (CRAN v0.2.0+) and `ellmer` for LLM interaction. Evaluation logs are written to the vitals log directory (set via `VITALS_LOG_DIR` env var or `vitals_log_dir_set()`). Use `vitals_view()` or the Inspect log viewer to browse results.

## Regenerating the PDF reference manual

When asked to regenerate `docs/reference_manual.pdf`, follow this exact procedure and overwrite existing contents in `docs/`:

1. Create a temp package directory with `R/`, `man/`, a minimal `DESCRIPTION`, and a stub `NAMESPACE`.
2. Copy all `R/*.R` files (excluding `R/legacy/`) into the temp package's `R/` dir, stripping any `library()`, `source()`, and `require()` lines.
3. Run `roxygen2::roxygenise()` on the temp package to generate `.Rd` files.
4. Add TinyTeX to PATH: `Sys.setenv(PATH = paste(file.path(tinytex::tinytex_root(), "bin", "windows"), Sys.getenv("PATH"), sep = ";"))`.
5. Run `R CMD Rd2pdf` from within R via `system2(file.path(R.home("bin"), "R"), ...)` with flags `--no-preview --force --no-clean --output=docs/reference_manual.pdf`.
6. If it fails at `makeindex`, find the `Rd2.pdf` in the `.Rd2pdf*` build directory under the project root and copy it to `docs/reference_manual.pdf`.
7. Clean up `.Rd2pdf*` directories and the temp package.

## Dev notes

`Ebb-dev/` is gitignored and contains local development notes and changelogs.
